---
description: Corre ESLint (next lint) y reporta errores sin intentar arreglarlos
---

Ejecutá `npm run lint 2>&1`.

- Sin warnings/errores → respondé "Limpio".
- Con warnings → listá los más importantes (rule + archivo:línea).
- NO arregles nada automáticamente salvo pedido explícito.
