{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import json,sys; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path','').replace('\\\\','/'); print('Tocaste src/lib/actions.ts: si hay capacidad/reservas en juego, usa adminDb.runTransaction.', file=sys.stderr) if 'src/lib/actions.ts' in p else None\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import json,sys,subprocess; d=json.load(sys.stdin); cmd=d.get('tool_input',{}).get('command',''); sys.exit(subprocess.call(['bash','scripts/check-no-secrets.sh'])) if cmd.startswith('git commit') else None\""
          }
        ]
      }
    ]
  }
}
